<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <com.youth.banner.Banner
        android:id="@+id/banner"
        android:layout_width="match_parent"
        android:layout_height="170dp"
        app:delay_time="4000"
        app:indicator_width="5dp"
        app:indicator_height="5dp"
        app:indicator_margin="2dp"
        app:indicator_drawable_selected="@drawable/indicator_selected_default"
        app:indicator_drawable_unselected="@drawable/indicator_unselected"/>
    <LinearLayout
        android:id="@+id/v_menu"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingBottom="10dp"
        android:paddingTop="20dp"
        android:background="@color/white">

    </LinearLayout>

    <include layout="@layout/layout_info_natal" />

    <include layout="@layout/layout_quizzes_menu" />

    <View android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/white"
        android:layout_marginTop="@dimen/home_base_space"
        android:layout_marginBottom="@dimen/home_base_space"/>
</LinearLayout>